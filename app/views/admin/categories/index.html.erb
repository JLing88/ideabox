<h1>Admin Categories</h1>

<h3> <%= link_to "Create Category", new_admin_category_path %></h3>
<div>
  <ul>
    <% @categories.each do |category| %>
      <li> <%= link_to category.title, admin_category_path(category) %></li>
    <% end %>
  </ul>
</div>
